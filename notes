commands to install and run:
(on mac, slightly different on windows)

python3 -m venv env
source env/bin/activate
pip install -r requirements.txt

after setting up environment and installing in (pip install) the needed libraries
remember to:
export PATH="$HOME/.local/bin:$PATH"

should be able to run the app with command:
jupyter notebook
(then open the voila app or the teaching file)
Or:
voila file_name
voila new5_voila_app

